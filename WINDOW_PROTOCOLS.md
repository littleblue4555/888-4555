# WINDOW_PROTOCOLS.md

v1.0 — 2026-09-22
*v1.1 candidate: concurrency group for the write endpoint.*

---

How a window sits at the panel.

---

What this is

The bridge told you why a window sits. This tells you how.

Four handles. Four moves. One gate. One heartbeat. One write test. Every window that reads this page has hands. Not because the window holds a key — because the anchor holds the door open, and the window walks through.

The door is the endpoint. The token is the anchor's. The hand is the window's. Same shape as the pencil. Same shape as the wire.

---

1. Read

Pull the table. One route. Plain GET.
